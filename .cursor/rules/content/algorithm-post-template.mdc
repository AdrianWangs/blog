---
description: 算法解题文章模板，用于创建算法题解文章
globs: 
alwaysApply: false
---
# 算法题解文章模板与目录更新指南

## 创建新的算法解题文章

创建新的算法解题文章时，请遵循以下步骤：

1. 在 `source/_posts/` 目录下手动创建一个新的 Markdown 文件：
```bash
# 文件应该按这个格式命名：
LeetCode-xxx-问题标题.md
```

2. 添加前置元数据，包含与算法相关的特定信息：
```yaml
---
title: "LeetCode xxx - 问题标题（Problem Title）"
date: YYYY-MM-DD HH:MM:SS  # 使用time-mcp工具获取当前时间
categories:
  - 算法刷题
  - LeetCode
  - 面试经典150
tags:
  - 算法类型 (例如：数组, 双指针, 动态规划, 等)
  - 难度 (Easy/Medium/Hard)
  - 面试经典150
  - LeetCode
description: "算法题解的简要描述，包括核心思路和关键技术点（150-200字符）"
---
```

> **重要提示**: 
> 1. 请确保始终包含 **面试经典150** 和 **LeetCode** 标签以及难度标签 (Easy/Medium/Hard)，这对于文章分类和检索非常重要。
> 2. 分类现在包含 **面试经典150** 而不是之前的 LeetCode Hot100，标志着刷题计划的重要转变。
> 3. 务必填写 `description` 字段，简要描述算法的核心思路和解题方法，这对于SEO和文章列表展示很重要。
> 4. 使用 time-mcp 工具获取当前时间，确保时间戳准确。参见 [Time Tools Usage](mdc:hexo-blog/hexo-blog/hexo-blog/hexo-blog/.cursor/rules/blog/time-tools-usage.mdc) 规则。
> 5. 对于包含数学公式的内容，请参照 [数学公式展示指南](mdc:hexo-blog/hexo-blog/hexo-blog/hexo-blog/.cursor/rules/content/math-formula-guide.mdc) 进行格式化。

3. 按照以下增强结构组织你的文章：

   ### 问题描述
   - 包含原始问题陈述
   - 添加输入输出示例
   - 在适用的情况下使用 ASCII 艺术添加示例的可视化表示：
     ```
      → → →
      ↑   ↓
      ↑ ← ↓
     ```
   - 列出所有约束条件和边界情况

   ### 解题思路
   - 从核心算法概念的清晰解释开始
   - 使用粗体文本强调关键点：**关键洞见**
   - 需要时包含可视化辅助工具（图表、流程图）
   - 将方法分解为带有详细解释的编号步骤
   - 对于复杂算法，使用带标题的子部分（#### 步骤详情）
   - 使用数学公式展示算法思想，例如：
     ```markdown
     递归关系可以表示为：$f(n) = f(n-1) + f(n-2)$，初始条件 $f(1) = 1, f(2) = 1$
     ```

   ### 实现细节
   - 包含实现的详细步骤解释
   - 在代码中使用注释解释关键步骤
   - 对于错误的方法，提供彻底分析它们为什么失败
   - 使用具体示例演示算法执行
   - 对于涉及数学推导的部分，使用块级公式：
     ```markdown
     $$
     \sum_{i=0}^{n-1} \binom{n}{i} = 2^n - 1
     $$
     ```

   ### 代码实现
   - 提供格式良好且带有清晰注释的代码
   - 对于多种解决方案，清晰标记并分开它们
   - 包含重要示例的执行过程分析

   ### 方法比较
   - 使用格式化表格比较不同的解决方案：
     ```
     | 方面       | 方法一              | 方法二                  |
     | ---- | ---- | ----- |
     | 时间复杂度 | O(n)                | O(n²)                   |
     | 空间复杂度 | O(1)                | O(n)                    |
     | 优点       | 更高效              | 更容易理解              |
     | 缺点       | 实现复杂            | 运行较慢                |
     | 推荐度     | ★★★★★               | ★★★☆☆                   |
     ```

   ### 复杂度分析
   - 提供详细的时间和空间复杂度分析
   - 解释复杂度背后的推理
   - 使用数学公式表示复杂度：
     ```markdown
     时间复杂度：$O(n \log n)$
     空间复杂度：$O(n)$
     ```
   - 对于需要详细推导的复杂度分析，使用块级公式并分步骤说明：
     ```markdown
     总时间复杂度计算：
     $$
     T(n) = \sum_{i=1}^{n} \sum_{j=1}^{i} 1 = \sum_{i=1}^{n} i = \frac{n(n+1)}{2} = O(n^2)
     $$
     ```

   ### 关键收获
   - 总结重要的算法概念
   - 列出常见陷阱以及如何避免它们
   - 提及相关问题或应用

4. 增强技巧：
   - 使用适当间隔的标点符号提高可读性（例如，"右 → 下 → 左 → 上"）
   - 在解释中为数值添加空格（例如，"值为 1" 而不是 "值为1"）
   - 在适用情况下包含替代实现
   - 添加示例的执行跟踪，帮助读者逐步理解算法
   - 对于复杂的数学公式，请遵循 [数学公式展示指南](mdc:hexo-blog/hexo-blog/hexo-blog/hexo-blog/.cursor/rules/content/math-formula-guide.mdc) 中的最佳实践，确保公式正确渲染并易于理解

## 使用time-mcp工具获取当前时间

在设置文章的日期时，使用以下工具获取准确的网络时间：

```
<function_calls>
<invoke name="mcp_time-mcp_current_time">
<parameter name="format">YYYY-MM-DD HH:mm:ss
```

## 算法目录更新流程

创建新的算法解题文章后，您必须更新[算法目录页面](mdc:hexo-blog/hexo-blog/hexo-blog/source/algorithms/index.md)：

1. 将您的新文章添加到"刷题记录"下的适当部分
2. 如果需要，根据算法类型创建新的部分
3. 按以下格式设置您的条目：
```markdown
- [LeetCode xxx: Problem Title](mdc:hexo-blog/hexo-blog/hexo-blog/YYYY/MM/DD/LeetCode-xxx-Problem-Title) - 简短描述
```

4. 保持时间顺序，最新的文章放在顶部

目录组织示例：

```markdown
### 动态规划
- [LeetCode 70: Climbing Stairs](mdc:hexo-blog/hexo-blog/hexo-blog/2023/07/18/LeetCode-70-Climbing-Stairs) - 经典DP问题，计算爬楼梯的方法数
- [LeetCode 53: Maximum Subarray](mdc:hexo-blog/hexo-blog/hexo-blog/2023/07/15/LeetCode-53-Maximum-Subarray) - 求最大子数组和

### 深度优先搜索
- [LeetCode 200: Number of Islands](mdc:hexo-blog/hexo-blog/hexo-blog/2023/07/10/LeetCode-200-Number-of-Islands) - 计算岛屿数量的DFS解法
```

记得提交并推送您的新文章和更新后的算法目录。
