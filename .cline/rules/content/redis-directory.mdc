---
description:
globs:
alwaysApply: false
---
# Redis 文章目录结构

本文档定义了博客中 Redis 相关文章的目录结构和组织方式。

## 基本目录结构

Redis 技术文章应按照以下目录结构进行组织：

```
source/_posts/八股文/REDIS/
├── 基础知识/         # Redis 基础概念和特性
│   ├── 数据结构/     # Redis 核心数据结构
│   ├── 持久化/       # RDB 和 AOF 持久化
│   └── 事务/         # Redis 事务相关
├── 高级特性/         # Redis 高级功能
│   ├── 分布式锁/     # Redis 分布式锁实现
│   ├── 消息队列/     # Redis 作为消息队列
│   └── 缓存设计/     # 缓存策略和模式
└── 集群/             # Redis 集群相关
    ├── 主从复制/     # 主从复制机制
    ├── 哨兵模式/     # Sentinel 哨兵架构
    └── 集群模式/     # Redis Cluster
```

## 文章命名规范

- 文件名应使用中文，清晰表达文章主题
- 同一主题的多篇文章可以使用数字编号，如：`缓存击穿.md`、`缓存雪崩.md`、`缓存穿透.md`

## 分类和标签

### 分类设置

所有 Redis 文章应至少包含以下分类路径：
```yaml
categories:
  - 八股文
  - REDIS
  - [具体分类，如：基础知识、高级特性、集群]
```

### 标签设置

标签应包含：
```yaml
tags:
  - REDIS
  - [具体特性，如：持久化、分布式锁、集群等]
```

## 添加新文章到目录

添加新的 Redis 文章时，应遵循以下步骤：

1. 确定文章所属的分类目录
2. 使用 Hexo 创建新文章：`hexo new post "八股文/REDIS/[分类]/[文章名]"`
3. 在文章 Front-matter 中设置正确的分类和标签
4. 文章创建后会自动生成在对应目录

## 更新主菜单

在 Redis 相关内容添加完成后，需要更新 `_config.vivia.yml` 中的 menu 配置，添加 Redis 菜单项：

```yaml
menu:
  首页: /
  Leetcode题解: /algorithms
  MySQL: /mysql
  Redis: /redis  # 新增 Redis 菜单项
  归档: /archives
  关于我: /about
```
